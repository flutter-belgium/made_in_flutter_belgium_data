name: api_deploy
description: A sample command-line application.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=2.19.2 <3.0.0'

dependencies:
  path: ^1.8.0
  api_validate:
    path: ../api_validate

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
